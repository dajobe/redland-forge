# Build Redland TUI Dependencies
# Core TUI library for terminal interface
blessed>=1.20.0

# Additional dependencies that might be needed for full functionality
# (These are optional and can be added as needed)

# For enhanced terminal features (optional)
# blessed-terminal>=1.0.0

# For better async support (optional)
# asyncio-mqtt>=0.11.0

# For enhanced color support (optional)
# colorama>=0.4.6

# For better SSH integration (optional)
paramiko>=3.3.1
# fabric>=3.0.0 
